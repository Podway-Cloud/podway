## 1. Conceptual Visuals And Release Truth

- [x] 1.1 Storyboard the useful-app, automation, and bot example outcomes, choose the default app
  concept, and freeze concise typed copy for all three states.
- [x] 1.2 Create, crop, optimize, and add dimension-stable conceptual assets for the three hero
  outcomes and six catalog starters using one consistent visual system; retain editable source or
  generation notes for later replacement.
- [x] 1.3 Add a persistent readable "Example project" treatment to every conceptual hero outcome and
  verify none contains customer attribution, testimonials, usage metrics, or live-capture cues.

## 2. Hero And Access Flow

- [x] 2.1 Build the server-rendered landing shell with the stable outcome-led hero, concise support
  copy, signed-out access CTA, signed-in dashboard CTA, and no terminal-led primary visual.
- [x] 2.2 Build the isolated client-side example component with fixed app → automation → bot order,
  typed example copy, synchronized outcome asset, manual segmented controls, stable dimensions,
  and pause-after-interaction behavior.
- [x] 2.3 Add keyboard semantics, visible focus, stable assistive text, and a reduced-motion mode that
  disables typing/autoplay while retaining manual example selection.
- [x] 2.4 Add focused access-flow coverage proving signed-out primary CTA → sign-in and signed-in
  primary CTA → dashboard.

## 3. Starter Catalog

- [x] 3.1 Add the responsive six-item starter catalog using the final outcome-led names,
  nontechnical descriptions, and conceptual starter imagery.
- [x] 3.2 Make starter interactions keyboard accessible and verify all six items remain scannable at
  320px without overflow, clipped text, or layout shifts.

## 4. Product Proof And Differentiation

- [x] 4.1 Add one concrete proof section covering what Podway prepares (services, data, secrets,
  public preview, persistence, and scheduling) without repeating the hero promise.
- [x] 4.2 Add one consolidated differentiation section covering persistent projects,
  browser/mobile reachability, full-workspace control, and precise BYO Claude/Codex subscription
  positioning with no token markup.
- [x] 4.3 Add the short closing CTA and update title, description, Open Graph, and social metadata to
  the outcome-led positioning.

## 5. Interaction Measurement

- [x] 5.1 Define stable event names and item identifiers for primary CTA, manual example selection,
  and starter selection in a small vendor-neutral landing analytics adapter.
- [x] 5.2 Connect the landing interactions to the adapter and test that an unconfigured backend is a
  no-op that never blocks navigation or interaction.

## 6. Responsive And Visual Verification

- [x] 6.1 Add or update automated landing coverage for deterministic selection, pause behavior,
  reduced motion, metadata, and absence of horizontal overflow.
- [x] 6.2 Run unit tests, the relevant end-to-end flow, type checking, and the production web build;
  resolve all failures introduced by the change.
- [ ] 6.3 Inspect Playwright screenshots at wide desktop, laptop, tablet, and 320px/390px mobile
  viewports in default and reduced-motion modes; verify assets load, text fits, controls remain
  reachable, and no animated layers overlap copy or CTA.
- [x] 6.4 Review every public claim against the shipped pre-alpha behavior and ToS constraints, then
  update `docs/positioning.md` and the landing block in `docs/pre-alpha-plan.md` with the final copy
  decisions and completion state.
