## 1. Focused Agent-Home Composition

- [x] 1.1 Build the signed-in-aware `agent-home` server composition with the focused hero promise,
  dominant “Give my agent a home” CTA, accurate access cues, and a single request-to-running-system
  proof labeled as simulated product data.
- [x] 1.2 Add distinct capability, operating-loop, service-assembly, ownership/trust, and closing-CTA
  sections without reintroducing the playbook catalog or making unsupported deployment/database
  claims.

## 2. Conversion-Led Visual System

- [x] 2.1 Implement an isolated responsive CSS module with an architectural workspace graphic,
  high-contrast CTA hierarchy, stable status layout, and coherent desktop/tablet/mobile composition.
- [x] 2.2 Add visible keyboard focus, non-color status labels, stable dimensions, and reduced-motion
  behavior; verify the page has no horizontal overflow at 320px.

## 3. Isolated Preview Surface

- [x] 3.1 Add `/preview/landing/agent-home` with `noindex` metadata and `/` canonicalization, using
  non-instrumented navigation so review traffic cannot emit current experiment interactions.
- [x] 3.2 Add focused automated coverage proving the preview remains outside the current variant
  allowlist, renders the new narrative/CTA, and exposes the required metadata.

## 4. Verification And Integration Safety

- [x] 4.1 Run targeted landing tests, lint/type checks, and a production web build; resolve failures
  without changing the current experiment definition or its two existing landing compositions.
- [x] 4.2 Inspect desktop and 320px screenshots plus keyboard/reduced-motion behavior, fix visual
  issues, and confirm all work remains isolated on the dedicated worktree branch for later rebase.
