{
  "name": "@podway/relay",
  "version": "0.2.4",
  "description": "Podway relay \u2014 runs a fetch relay on your machine so a pod can read pages from your network, using sessions you sign into. Install: npm i -g @podway/relay, then `relay start`.",
  "type": "module",
  "bin": {
    "relay": "./dist/main.js"
  },
  "files": [
    "dist",
    "README.md",
    "FAQ.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "playwright": "^1.49.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "@types/ws": "^8.5.13",
    "typescript": "^5.7.3",
    "vitest": "^2.1.9"
  },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/podway-cloud/relay.git"
  },
  "homepage": "https://github.com/podway-cloud/relay#readme",
  "bugs": {
    "url": "https://github.com/podway-cloud/relay/issues"
  }
}
