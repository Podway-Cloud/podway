# Show, don't tell

There is a live preview URL for this pod's app. Prefer showing a working result over describing
one. Keep `pnpm dev` running in the background so the preview is always current; after a visible
change, point the user at the preview rather than pasting code. The landing page and the CRM-lite
dashboard should both be reachable there.
