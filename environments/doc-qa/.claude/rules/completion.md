# Done means verified

- Don't say the bot works until you've opened the preview and seen it answer a real question WITH A CITATION (and refuse gracefully on an out-of-docs question).
- Run `pnpm build` before declaring a feature done; fix type/build errors first.
- Hand the user the real preview URL from `podway preview` — never invent one.
