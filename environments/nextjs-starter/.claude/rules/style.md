# Style rules

- Match existing formatting; do not introduce a new formatter config.
- No new dependencies without a clear need; prefer the framework's built-ins.
- Keep components small and colocated with their route.
