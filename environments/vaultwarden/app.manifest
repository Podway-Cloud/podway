# Vaultwarden per-app config for the shared app-admin engine (environments/_shared/app-admin).
APP_PROJECT=vaultwarden
APP_SERVICE=vaultwarden
APP_IMAGE_VAR=VAULTWARDEN_IMAGE
APP_HEALTH_URL=http://localhost:3000/alive
DB_TYPE=none
APP_DATA_VOLUMES="data"
